global:
  resolve_timeout: 5m

route:
  group_by: ["alertname", "job"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2h
  receiver: default-webhook

receivers:
  - name: default-webhook
    webhook_configs:
      - url: http://127.0.0.1:65535/
        send_resolved: true
